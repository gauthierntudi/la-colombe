import 'package:flutter/material.dart';

/// Aligné sur `--primary-color` du dashboard admin.
abstract final class AppColors {
  static const primary = Color(0xFF0D30F5);
  static const primarySoft = Color(0xFFE8ECFE);
}
